# 规划

## TTS

使用 CosyVoice3，只能调用 voice/CosyVoice/modules.py

## LLM

暂时使用占位函数，传入语料内容，返回固定文本

## 组织

### 用户

- 使用 OIDC 登录，登录和鉴权暂时使用占位中间层
- 首次登录强制设置仅包含大小写字母和数字的唯一 userId
- 允许设置用户名
- 后端管理使用从 1 开始的自增 id
- 路由为 /user/<userId>

### 音色

- 拥有唯一 id，从 1 开始自增
- 拥有标题
- 拥有标签，标签分为几类
    - author：用户 userId
    - gender
- 拥有示例音频，可以是原音频，也可以由 author 设置其他公开的音频作为示例
- 存储在 data/voice/<id>
- 路由为 /voice/<id>

### 音频

- 拥有唯一 id，从 1 开始自增
- 拥有标题
- 拥有标签，标签分为几类
    - author：用户 userId
    - speaker，允许多个
    - topic，允许多个
    - category（题型）
- 拥有文本
- 存储在 data/audio/<id>
- 路由为 /audio/<id>

### 标签

- 音色和音频的标签分为独立的两套系统，都拥有唯一 id，从 1 开始自增
- 使用英文作为主语言，所有标签必须有英文版本，支持多语言，在音色和音频的展示页面，根据用户语言展示，搜索时必须使用英文
- 标签不能含有空格，统一用下划线表示空格，在音色和音频的展示页面将下划线显示为空格
- 拥有类型，一个完整的标签由类型和内容组成，比如 gender:male（因此标签内容不能含有冒号），可以简写为 g:male，同理还有 a, s, t, c 的简写，不同类型在信息展示页面分行显示，在搜索时可以输入包含类型和内容的完整标签以更精确地搜索
- 搜索时，用空格隔开关键词
- 搜索时，还可以输入标签的多语言版本，并提供联想补全，补全只能补全英文完整标签
