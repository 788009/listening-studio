# 英语听力生成工具实施规划

## 子任务清单

### 阶段 A：工程基础

#### A01 建立后端工程骨架

**前置依赖：** 无。

**本次实现：**

- 创建 `backend/app` 包、应用工厂和 `backend/app/main.py`。
- 添加 `/health/live`，只表示进程存活；添加统一 JSON 错误响应骨架。
- 创建依赖清单和项目命令文档，固定 Python 3.10 版本范围。
- 建立项目 `.gitignore`，排除数据库、日志、`data/` 运行数据、模型、Python 缓存、`node_modules/`、`frontend/dist/`、测试缓存和环境文件。

**测试与验收：**

- 应用可导入，测试客户端访问 `/health/live` 返回 200 和稳定 JSON 结构。
- 启动不导入或加载 CosyVoice。

#### A02 配置管理与 Loguru 日志

**前置依赖：** A01。

**本次实现：**

- 用 Pydantic Settings 定义数据库地址、数据目录、日志目录、上传限制、调试认证开关和模型目录。
- 增加 Loguru 初始化：文件记录 DEBUG 及以上，按大小轮转并保留固定份数；终端仅输出 INFO、WARNING、ERROR 和关键生命周期事件。
- 增加请求日志中间件，生成或透传 request ID，记录方法、规范化路径、状态码和耗时。
- 对敏感请求头、Cookie、文本正文和上传内容做日志排除。

**测试与验收：**

- 临时目录测试能生成日志文件，异常请求包含同一个 request ID。
- 测试日志中不出现认证头、Cookie 或正文原文。

#### A03 数据库会话和迁移框架

**前置依赖：** A01、A02。

**本次实现：**

- 配置 SQLAlchemy 2 engine、声明基类、事务会话依赖和 SQLite 外键约束。
- 初始化 Alembic，建立空基线迁移和迁移运行说明。
- 增加 `/health/ready`，检查数据库连接和数据目录可写性，不检查 GPU 模型。
- 测试使用独立临时 SQLite 数据库，禁止接触开发数据库。

**测试与验收：**

- 全新数据库可升级到最新版本并降回基线。
- 数据库不可用时 readiness 返回 503，异常写入日志。

#### A04 统一异常、校验和 API 响应约定

**前置依赖：** A01、A02。

**本次实现：**

- 定义 `not_found`、`forbidden`、`conflict`、`validation_error`、`job_failed` 等领域异常。
- 统一错误结构为 `code`、`message`、`details`、`request_id`，不向客户端返回堆栈。
- 定义分页参数和响应：`items`、`page`、`page_size`、`total`。
- 统一标题、语言代码、可见性和整数 ID 的 Pydantic 校验器。

**测试与验收：**

- 领域异常与请求校验分别映射到稳定状态码和错误码。
- 非预期异常返回 500，终端和文件均有异常记录，响应不泄漏路径或堆栈。

#### A05 建立 Vue 3 前端工程

**前置依赖：** A01、A04。

**本次实现：**

- 使用 Vite 创建 Vue 3 + TypeScript 工程，安装并配置 Vue Router、Pinia 和 Tailwind CSS，锁定 Node 与包管理器版本。
- 建立应用壳、路由占位页、404 页、类型化 API 客户端和统一 API 错误处理；所有后端请求使用 `/api` 相对路径。
- 配置 Vite 开发代理和 FastAPI 生产静态托管：生产环境返回 `frontend/dist`，`/api`、`/auth`、媒体及健康检查路由不进入 SPA fallback。
- 配置 ESLint、TypeScript 检查、Vitest 和 Vue Test Utils；Tailwind 主题使用简洁、冷静的中性色与有限状态色，不建立一套单色界面。

**测试与验收：**

- `npm run dev` 可访问前端并通过代理读取 `/health/live`；刷新任意 Vue 前端路由不会返回 404。
- `npm run typecheck`、`npm run lint`、`npm run test` 和 `npm run build` 通过，构建产物不进入 Git。

### 阶段 B：身份与用户

#### B01 用户模型与首次迁移

**前置依赖：** A03、A04。

**本次实现：**

- 建立 `users` 表：自增 ID、issuer、subject、待完善状态、`user_id`、规范化 `user_id`、username、locale、时间戳。
- 对 `(issuer, subject)` 和非空规范化 `user_id` 建唯一约束。
- `userId` 校验为非空 ASCII 字母数字；规范化值使用小写，避免仅大小写不同的重复路由。
- 建立用户 repository 和服务层，不添加页面。

**测试与验收：**

- 首条记录 ID 为 1；重复身份和大小写不同的重复 `userId` 均被拒绝。
- username 可重复，`userId` 设置后服务层拒绝再次修改。

#### B02 占位 OIDC 认证中间层

**前置依赖：** B01。

**本次实现：**

- 定义与真实 OIDC 解耦的 `IdentityProvider` 协议和 `PlaceholderIdentityProvider`。
- 调试模式通过受控请求头或签名会话注入 `issuer`、`subject`；关闭调试模式时拒绝该入口。
- 首次认证按身份创建待完善用户，并把当前用户注入请求上下文。
- 增加 `require_teacher` 和 `require_completed_profile` 依赖；匿名请求保持学生身份。

**测试与验收：**

- 匿名、待完善教师、已完善教师三种状态权限明确。
- 客户端伪造用户数据库 ID 或 `userId` 不能切换身份；关闭调试认证后占位头无效。

#### B03 用户 API 与 `/user/<userId>` 页面

**前置依赖：** B02、A05。

**本次实现：**

- 实现首次设置 `userId`、修改 username 和 locale 的 API。
- 实现用户公开摘要查询，并按权限过滤私有统计信息。
- 在 Vue Router 中实现 `/user/:userId` 页面和首次登录强制设置页面。
- 所有教师功能检测待完善状态，API 返回专用错误码，前端路由守卫将待完善教师导航到设置页面。

**测试与验收：**

- 首次设置后路由可访问；非法字符、重复值和二次修改被拒绝。
- 修改 username 不改变作者关系、作者标签和用户路由。

#### B04 资源授权策略

**前置依赖：** B02、B03。

**本次实现：**

- 建立集中式授权服务，提供查看、编辑、删除、设为公开、作为示例、用于合成等判定。
- 明确学生只可查看 `public + ready` 音频；教师可查看公开音频、公开音色和自己的所有资源。
- 禁止教师修改其他教师资源，即使资源公开。
- 路由只调用授权服务，不在各处理函数复制条件表达式。

**测试与验收：**

- 用权限矩阵覆盖匿名、所有者和其他教师对公开、私有、处理中、失败资源的操作。
- 对无权资源读取统一返回 404，避免确认私有 ID 是否存在；已知公开资源上的非法修改返回 403。

### 阶段 C：标签领域

#### C01 音色标签数据模型

**前置依赖：** A03、B01。

**本次实现：**

- 建立音色标签和音色标签翻译表；音色标签关联表等 D01 创建音色表时一并建立。
- 类型只允许 `author`、`gender`；英文值与翻译值保存规范化搜索字段。
- 唯一约束覆盖类型加英文规范值、标签加语言。
- 编写迁移、repository 和基础 schema。

**测试与验收：**

- 音色标签 ID 从 1 自增；缺少英文、包含冒号、规范化后为空和重复翻译被拒绝，输入空白被规范化为下划线。
- 数据库外键和唯一约束在 SQLite 测试中实际生效。

#### C02 音频标签数据模型

**前置依赖：** A03、B01。

**本次实现：**

- 独立建立音频标签和翻译表，不复用 C01 的表或 ID；关联表等 E01 创建音频表时一并建立。
- 类型只允许 `author`、`voice`、`topic`、`category`。
- 提取仅包含纯规则的共享值对象和校验函数，repository 仍保持两套。
- 编写迁移和约束测试。

**测试与验收：**

- 音频标签也从 ID 1 开始，与音色标签相同数字不发生关联。
- 类型范围、英文必填和唯一性规则全部由服务层及数据库共同保护。

#### C03 标签规范化与解析器

**前置依赖：** C01、C02。

**本次实现：**

- 实现连续空白转单个下划线、英文小写规范化、下划线展示为空格和语言回退函数。
- 实现 `author/a`、`gender/g`、`voice/v`、`topic/t`、`category/c` 解析，按资源域拒绝无效类型。
- 实现按空白分词的查询解析器，输出类型标签词和普通关键词的结构化对象。
- 对未知前缀、空值、多个冒号和超长输入返回明确校验错误。

**测试与验收：**

- 单元测试覆盖完整类型、简写、大小写、多语言字符、连续空白和非法输入。
- 解析器不拼 SQL，不依赖数据库，也不直接返回用户输入的路径或表达式。

#### C04 标签管理 API

**前置依赖：** C03、B04、D01、E01。

**本次实现：**

- 分别实现音色标签和音频标签的创建、翻译增改、查询接口。
- 教师可创建非 author 标签；author 标签只由音色或音频资源服务按需创建。
- 删除仅允许删除未关联的非 author 标签；冲突返回使用数量。
- 返回结构同时包含英文规范值、请求语言展示值和完整英文标签。

**测试与验收：**

- 两套 API 不接受另一套类型或 ID。
- 并发创建相同规范标签时只有一个成功，另一个返回可恢复的冲突响应。

#### C05 标签联想补全

**前置依赖：** C03、C04。

**本次实现：**

- 分别为音色、音频实现补全端点，支持英文值、完整标签、简写和任意已有翻译作为输入。
- 输入翻译时先定位标签，再返回英文完整形式；结果不得返回翻译文本作为可提交值。
- 采用前缀优先、包含匹配次之的稳定排序，并限制结果数量。
- author 补全遵守资源可见性和用户公开信息边界。

**测试与验收：**

- 中文等翻译输入能返回 `topic:english_value`，而不是本地化标签。
- 跨域类型、私有作者信息和超过上限的结果不会泄漏。

### 阶段 D：音色

#### D01 音色模型、状态与存储服务

**前置依赖：** B04、C01。

**本次实现：**

- 建立 `voices` 表及音色标签关联表：作者、标题、规范化搜索标题、状态、可见性、示例模式、示例音频 ID、错误摘要和时间戳。示例音频外键在 E01 创建音频表后通过后续迁移添加。
- 建立 `data/voice/<id>` 路径服务，提供临时写入、原子替换、存在性检查和安全删除。
- 创建音色时确保对应音色 author 标签存在并自动关联；author 标签值来自 `userId`。
- 增加合法状态迁移规则：`pending -> processing -> ready/failed`。

**测试与验收：**

- 第一条音色 ID 和目录均为 1；路径不能被标题、文件名或请求参数影响。
- 非 ready 音色不能公开或用于合成，非法状态回退被拒绝。

#### D02 CosyVoice 单一适配层

**前置依赖：** A02、D01。

**本次实现：**

- 在 `backend/app/integrations/cosyvoice.py` 解决本地模块导入，方式以 `voice/modules_test.py` 验证过的路径处理为准。
- 只导入 `voice/CosyVoice/modules.py` 的音色提取和语音合成接口，封装为 `extract_voice`、`synthesize`。
- 适配层验证模型目录、输入文件和输出父目录，并把底层异常转成领域异常。
- 定义可注入协议和 fake 实现，使普通测试不加载 CUDA、vLLM 或模型。

**测试与验收：**

- 导入 FastAPI 应用和运行普通测试时不加载真实模型。
- fake 集成测试验证参数和目标路径；另提供显式标记的 GPU smoke test，不默认运行。

#### D03 音色上传和创建服务

**前置依赖：** D01、D02、C04。

**本次实现：**

- 实现参考音频上传校验：大小、扩展名、实际媒体格式、时长上下限和空文件检查。
- 服务先创建音色记录和临时任务文件，再调用适配层生成 `voice.pt`，保存规范化 `reference.wav`。
- 支持标题、gender 标签和目标可见性；author 标签由系统注入，目标为公开时也要等生成成功后才能公开。
- 失败时设置 `failed`、清理临时文件并保留简短错误摘要供所有者查看。

**测试与验收：**

- fake TTS 下成功生成固定目录结构；非法媒体在模型调用前被拒绝。
- 模型异常后没有最终 `voice.pt`，资源不能播放或公开，日志包含 voice ID 和 request ID。

#### D04 音色查询、更新与删除 API

**前置依赖：** D03、B04。

**本次实现：**

- 实现音色列表、详情、标题修改、gender 标签替换、可见性修改和删除；标题修改时同步更新规范化搜索标题。
- 列表支持分页、作者、状态、可见性、标签筛选和标题模糊搜索；结构化标签精确匹配，无类型词按标题子串或标签值匹配。
- 设为公开前检查 `ready` 和示例可播放；更新标签使用一个数据库事务。
- 删除前检查音频话轮、进行中任务和示例引用；存在引用时返回结构化冲突。

**测试与验收：**

- 所有者与其他教师的权限符合 B04；author 标签不能由更新接口改变。
- 删除成功后数据库关联和 `data/voice/<id>` 均不存在；删除失败不形成半删除状态。

#### D05 音色示例来源

**前置依赖：** D04、E02。

**本次实现：**

- 实现示例来源在 `original` 和 `public_audio` 之间切换。
- 为 `voices.sample_audio_id` 添加指向音频表的外键迁移，并明确删除限制。
- 选择音频时验证音频为 `public + ready`；保存外键并由播放接口动态解析。
- 被引用音频改为私有或删除时阻止操作，并返回引用该音频的音色列表给所有者。
- 原始参考录音只允许音色所有者和有权使用该音色的教师播放；匿名不获得原始上传文件。

**测试与验收：**

- 不能选择私有、失败或不存在的音频作为示例。
- 切回原始示例后解除音频引用；音频文件不被复制到音色目录。

#### D06 音色列表和详情页面

**前置依赖：** D04、D05、C05、A05。

**本次实现：**

- 使用 Vue Router 实现教师音色列表和 `/voice/:id` 详情视图，并用类型化 API 客户端加载数据。
- 页面展示标题、作者、状态、按类型分行的本地化标签、示例播放器和可见性。
- 所有者获得编辑、删除、示例切换控件；其他教师只获得允许的使用入口。
- 使用 SVG 图标、明确焦点样式、错误摘要和异步任务状态，不用 emoji。

**测试与验收：**

- 桌面和移动宽度下无文本或控件重叠，键盘可操作表单和对话框。
- 页面权限与 API 一致，直接访问私有音色不泄漏内容。

### 阶段 E：音频基础

#### E01 音频模型、话轮和存储服务

**前置依赖：** B04、C02、D01。

**本次实现：**

- 建立 `audios`、`audio_utterances`、音频标签关联表及状态迁移；音频保存规范化搜索标题，并支持单人、多话轮、语料生成和组卷来源类型。
- 建立 `data/audio/<id>/audio.wav` 存储服务和任务临时路径。
- 创建音频时自动关联音频 author 标签；voice、topic、category 标签由请求或生成流程关联。
- 保存音频格式、时长、采样率等可验证元数据。

**测试与验收：**

- 多话轮顺序、音色外键和级联规则正确；全文生成顺序稳定。
- 文件路径只由 audio ID 决定，状态不是 ready 时播放服务拒绝访问。

#### E02 音频查询、更新、可见性和删除 API

**前置依赖：** E01、C04。

**本次实现：**

- 实现列表、详情、标题修改、topic/category 标签更新、可见性修改和删除；生成流程维护 voice 标签，标题修改时同步更新规范化搜索标题。
- author 与实际 owner 保持一致，客户端不能修改。
- 播放端点支持 Range 请求、正确 MIME、缓存控制和权限检查。
- 删除前检查音色示例、组卷条目和运行中任务引用。

**测试与验收：**

- 匿名只能读取和播放公开 ready 音频；私有文件不能通过静态路径绕过权限。
- Range、无效范围、空文件和缺失文件返回正确状态；引用冲突不会删除文件。

#### E03 音频搜索服务

**前置依赖：** C03、E02。

**本次实现：**

- 实现标题和音频标签的组合搜索；匿名查询自动追加公开 ready 条件。
- 结构化标签精确匹配类型和值；普通关键词匹配英文标签、翻译或标题的规范化子串，不把音频正文作为首版搜索字段。
- 多个查询词采用 AND，同一词可在多个字段中 OR 匹配；分页和排序稳定。
- 对查询长度、词数和分页上限设限制，所有数据库条件参数化，并转义标题查询中的 `%`、`_` 和转义符。

**测试与验收：**

- 覆盖英文、翻译、完整标签、简写、多 voice/topic、标题局部匹配、大小写、Unicode 规范化、SQL 通配字符和无结果场景。
- 无权限资源不影响 total、补全或分页侧信道；测试包含基本查询性能断言。

#### E04 公共音频浏览与 `/audio/<id>` 页面

**前置依赖：** E02、E03、C05、A05。

**本次实现：**

- Vue 根视图直接显示可搜索的公开音频列表，不制作营销落地页。
- 实现 `/audio/:id` 视图：标题、播放器、文本、作者和按类型分行的本地化标签。
- 添加统一搜索输入和标签补全；普通文字执行标题模糊搜索，完整标签执行精确筛选，补全提交值使用英文完整标签，显示值可本地化。
- 教师所有者在详情页获得编辑、可见性和删除操作。

**测试与验收：**

- 未登录学生能完成搜索、查看、播放公开音频；不能看到教师管理入口。
- 移动和桌面布局无重叠，播放器和搜索可用键盘操作，页面不暴露磁盘路径。

### 阶段 F：任务队列与直接生成

#### F01 持久化任务模型和单进程执行器

**前置依赖：** A03、A02。

**本次实现：**

- 建立 `jobs` 表：类型、所有者、状态、进度、输入摘要、结果引用、错误摘要、时间戳和取消标记。
- 实现独立 worker 进程中的单消费者数据库轮询队列，使用原子领取避免重复执行并序列化 GPU 操作；Web 请求只创建任务，不直接等待模型。
- 启动时把遗留 `running` 任务标为可重试或失败，并重新排队安全的 `queued` 任务。
- 提供任务查询和取消 API；取消只保证尚未开始的任务不执行，运行中任务记录取消请求并在阶段边界停止。

**测试与验收：**

- fake handler 下任务按顺序执行，状态转换和进度持久化。
- 重启恢复、处理器异常、重复消费和取消场景不会生成两份最终资源。

#### F02 音色创建改为后台任务

**前置依赖：** D03、F01、A05。

**本次实现：**

- 新增异步上传接口和创建页面；请求只执行验证、暂存、创建 voice/job 的短事务，然后立即返回 202。
- worker 调用 D03 的音色生成服务并更新 voice 与 job 状态。
- 上传临时文件以 job ID 命名，成功和失败均执行清理策略。
- Vue 创建视图通过 Pinia 管理提交状态并轮询任务端点更新进度，失败仅向所有者显示可操作的错误摘要。

**测试与验收：**

- API 响应不等待 fake 模型；任务完成后 voice 变为 ready。
- 重复提交、刷新页面和 worker 失败不造成相同 job 的重复模型调用。

#### F03 单人文本生成音频

**前置依赖：** D04、E01、F01。

**本次实现：**

- 实现选择一个可用音色、输入文本、标题、标签和可见性的创建 API。
- 请求事务创建 pending audio、单个 utterance 和 synthesize job。
- worker 用 CosyVoice 适配层生成临时 WAV，验证后原子写入 `data/audio/<id>/audio.wav`。
- 生成完成后提取时长和采样率，更新 audio/job；失败执行补偿。

**测试与验收：**

- 不允许使用他人私有、失败或处理中的音色；公开音色可由其他教师使用。
- fake TTS 测试参数、文本、目标文件、标签和状态完整；普通测试不加载模型。

#### F04 多人对话脚本校验与音频拼接

**前置依赖：** F03。

**本次实现：**

- 定义多人对话请求：至少一个话轮，每轮指定音色、speaker 显示名和非空文本。
- 每个话轮分别合成临时 WAV，统一采样率、声道和样本宽度，在话轮间加入可配置静音。
- 使用可靠音频库按顺序拼接，不用字节直接连接 WAV；最终只保留合并文件。
- 生成规范化全文和 voice 标签，任一话轮失败则整个音频失败并清理所有片段。

**测试与验收：**

- 用短 fixture WAV 验证顺序、总时长、静音和格式统一。
- 覆盖相同音色多轮、多个音色、无权音色、中途失败和取消。

#### F05 直接生成页面

**前置依赖：** F03、F04、D06、C05、A05。

**本次实现：**

- 使用 Vue 组件实现单人/多人分段控件，使用分段选择器切换模式。
- 多人模式可增加、删除、调整话轮顺序，并为每轮选择音色和 speaker。
- 表单支持标题、topic/category 标签、可见性；提交后进入任务状态视图。
- 成功后链接到 `/audio/{id}`，失败时保留可安全重试的表单数据。

**测试与验收：**

- 端到端测试覆盖创建、轮询、完成和失败重试。
- 长文本、长音色标题和移动宽度下控件不溢出，动态增删话轮不引发布局跳动。

### 阶段 G：语料批量生成

#### G01 LLM 协议和固定占位实现

**前置依赖：** A02。

**本次实现：**

- 定义 `ListeningContentGenerator` 协议，输入语料、题型集合、数量和语言设置。
- 定义结构化输出：标题、对话话轮、题型和建议 topic/category；用 Pydantic 严格校验。
- 占位实现根据请求数量返回确定性的固定英文文本，不访问网络。
- 记录调用 ID、输入长度、题型和数量，不记录完整语料。

**测试与验收：**

- 相同输入产生稳定、合法且数量正确的结果。
- 数量越界、空语料、未知题型及不合法实现输出在进入 TTS 前失败。

#### G02 语料上传和批次模型

**前置依赖：** G01、F01。

**本次实现：**

- 建立生成批次及批次条目表，记录所有者、题型、数量、状态、批量标签和各结果 audio ID。
- 支持文本、TXT 和明确编码的语料上传；限制大小，拒绝二进制和空内容。
- 创建 batch/job 后将语料暂存到 `data/jobs/<job_id>`，不把大段原文放入日志。
- 题型多选且数量有配置上限，批量标签只允许音频域的 topic/category。

**测试与验收：**

- 上传后立即返回 batch/job；非法编码、文件类型、数量和标签被拒绝。
- 教师只能读取自己的批次，临时文件名不采用上传文件名。

#### G03 LLM 到多音频的批处理编排

**前置依赖：** G02、F04。

**本次实现：**

- worker 先调用占位 LLM 生成结构化条目，再为每条创建独立 audio 和 utterances。
- 根据请求中的角色到音色映射合成音频；缺失映射在 TTS 前使批次失败。
- 每个条目单独记录成功或失败，批次进度按完成条目数更新。
- 批量标签与 LLM 建议标签合并去重，author 标签仍由系统添加。

**测试与验收：**

- 数量 N 准确生成 N 个条目；标题、文本、题型、标签和音频一一对应。
- 中途失败时已成功条目可保留并明确标记，失败条目可单独重试且不重复成功项。

#### G04 语料批量生成页面

**前置依赖：** G03、C05、A05。

**本次实现：**

- 使用 Vue 表单组件实现语料输入/上传、题型多选、数量输入、角色音色映射和批量标签控件。
- 提交后显示批次总进度和每个条目的状态、标题及错误。
- 支持对失败条目重试，对成功条目批量修改 topic/category 和可见性。
- 页面刷新后从持久化 batch 恢复状态，不依赖浏览器内存。

**测试与验收：**

- 端到端覆盖混合成功、重试、批量标签和批量公开。
- 数量上限、上传错误和缺少音色映射在提交前后均有明确反馈。

### 阶段 H：组卷

#### H01 组卷预设和数据模型

**前置依赖：** E02、B04。

**本次实现：**

- 建立 `paper_presets`、`papers` 和有序 `paper_items`，记录片头、题间静音、重复次数和片尾参数。
- 提供只读内置预设和教师自定义预设；自定义预设只对所有者可见。
- 组卷条目只能引用创建者有权查看的 ready 音频。
- 组卷本身保存标题、状态和最终 audio ID，最终结果仍是普通音频资源。

**测试与验收：**

- 顺序、重复次数和预设边界值受数据库及服务校验。
- 引用音频改私有或删除时遵守已有组卷引用策略，不产生悬空外键。

#### H02 组卷渲染任务

**前置依赖：** H01、F01、F04。

**本次实现：**

- 创建 paper render job，读取快照后的有序条目和预设参数。
- 对输入音频统一格式，插入片头、静音和重复段，输出到新的 audio ID。
- 生成可阅读的组合文本和来源追踪，不修改原音频。
- 失败或取消时清理临时拼接文件，并保持原音频和 paper 条目不变。

**测试与验收：**

- fixture 音频验证顺序、重复次数和最终时长。
- 渲染期间源音频状态变化有确定行为：任务使用创建时快照或在开始前明确失败。

#### H03 组卷页面

**前置依赖：** H02、E04、A05。

**本次实现：**

- 使用 Vue 组件实现可搜索音频选择、已选列表排序、预设选择和参数预览；候选搜索同时支持标题模糊匹配和标签精确筛选。
- 提交后显示渲染进度，成功后进入普通音频详情页。
- 显示无权、已删除或状态变化的条目并阻止无效提交。
- 排序同时支持键盘按钮，不只依赖拖拽。

**测试与验收：**

- 端到端覆盖选取、排序、预设、渲染和播放最终音频。
- 大量候选音频采用分页或增量搜索，不把完整库一次加载到页面。

### 阶段 I：管理体验和可靠性

#### I01 教师资源管理页

**前置依赖：** D06、E04、G04、H03、A05。

**本次实现：**

- 实现“我的音色”“我的音频”“生成批次”“组卷”四个标签页。
- 支持状态、可见性、标签和创建时间筛选，以及批量可见性和批量标签操作。
- 批量操作逐项授权并返回成功、冲突、失败明细，不因一个失败掩盖其他结果。
- 删除使用确认对话框，并提前展示引用冲突。

**测试与验收：**

- 大于一页的数据保持选择和筛选行为明确；批量操作不影响其他用户资源。
- 页面在窄屏下改为可扫描列表，不出现嵌套卡片和横向溢出。

#### I02 国际化和语言回退

**前置依赖：** B03、C04、E04、D06、A05。

**本次实现：**

- 定义支持语言配置和 locale 解析顺序：用户设置、请求头、英文默认值。
- 本地化固定 UI 文案、错误消息和日期格式；资源标题和正文不自动翻译。
- 标签按当前语言显示翻译，缺失翻译回退英文；标签搜索提交仍使用英文完整标签，普通关键词用于标题模糊搜索。
- HTML 设置正确 `lang`，API 接受明确 locale 参数但验证允许列表。

**测试与验收：**

- 至少覆盖英文和一种非英文 locale 的标签展示、回退和补全行为。
- 切换语言不改变 URL 中的资源 ID、查询的英文规范标签或已保存数据。

#### I03 文件与数据库一致性修复工具

**前置依赖：** D01、E01、F01。

**本次实现：**

- 实现只读扫描命令，报告缺失文件、孤立目录、遗留临时文件、状态不一致和悬空任务。
- 实现显式 `--apply` 修复模式，只执行可逆或规则明确的操作；默认不删除未知文件。
- 启动时仅处理已知任务临时文件和 `running` 状态，不做大规模自动删除。
- 每项修复记录资源 ID、原状态、目标状态和结果。

**测试与验收：**

- 在构造的不一致 fixture 上，dry-run 不修改磁盘或数据库。
- apply 可重复运行，第二次无新增修改；未知目录只报告不删除。

#### I04 安全加固

**前置依赖：** 所有写入和播放 API 已完成。

**本次实现：**

- 全面检查上传大小、MIME 嗅探、文件名忽略、路径穿越、SQL 参数化和 HTML 转义。
- 为会话写请求增加 CSRF 防护；Cookie 设置 HttpOnly、SameSite 和生产 Secure。
- 为登录、搜索、上传、生成和播放端点增加适度限流或并发限制。
- 配置安全响应头，确认私有音频不被代理或浏览器公共缓存。

**测试与验收：**

- 集成测试覆盖路径穿越、脚本标题、伪造 MIME、越权 ID、CSRF 和超限请求。
- 日志与错误响应不包含凭证、服务器绝对路径或完整私有文本。

#### I05 可观测性与运维命令

**前置依赖：** F01、I03。

**本次实现：**

- 增加结构化业务日志字段：request ID、job ID、user DB ID、资源类型和资源 ID。
- 增加任务队列长度、成功/失败计数和处理时长的内部指标端点；端点需受保护。
- 提供数据库迁移、启动 Web、启动 worker、扫描一致性和 GPU smoke test 的明确命令。
- 文档说明单 GPU 下只运行一个 TTS worker，Web 可独立扩展但 SQLite 部署不做多机承诺。

**测试与验收：**

- 从一个失败 job 可用 job ID 串联请求、worker 和资源日志。
- 健康检查不触发昂贵模型推理，指标不暴露标题、文本或用户凭证。

### 阶段 J：最终验证与交付

#### J01 全链路集成测试

**前置依赖：** I01 至 I05。

**本次实现：**

- 使用 fake OIDC、fake LLM 和 fake TTS 建立隔离的完整测试应用。
- 覆盖教师首次登录、建音色、单人/多人生成、语料批量生成、公开、学生搜索播放、组卷和删除冲突。
- 验证每条链路的数据库记录、磁盘文件、标签、权限、任务状态和日志。
- 将测试数据全部放在临时目录，测试结束不污染 `data/` 和开发数据库。

**测试与验收：**

- 全链路测试可在无 GPU、无网络环境稳定重复运行。
- 失败断言能定位到具体业务阶段，不依赖固定测试执行顺序。

#### J02 前端浏览器验证

**前置依赖：** J01。

**本次实现：**

- 用 Playwright 覆盖 Vue 应用中匿名学生和教师的主要流程，并用 Vitest、Vue Test Utils 覆盖关键组件状态。
- 在常用桌面和移动视口截图检查导航、表单、对话话轮、标签、播放器、任务状态和管理列表。
- 检查键盘导航、焦点可见性、表单标签、颜色对比和动态状态的可访问提示。
- 修复阻塞流程、文本溢出、控件重叠和无权限入口显示问题，不在本任务重做视觉体系。

**测试与验收：**

- 主要流程没有浏览器控制台错误、失败请求或布局重叠。
- 页面可在禁用动画偏好和窄屏下使用，长英文标签不会撑破容器。

#### J03 真实 CosyVoice GPU 验收

**前置依赖：** J01；本机 GPU、模型和 vLLM 可用。

**本次实现：**

- 用专用测试用户和短参考音频执行一次音色提取。
- 分别执行单话轮和双话轮短文本生成，验证输出 WAV 可解析、时长非零且目录符合 ID 规则。
- 验证 worker 串行运行、失败清理和日志链路；不在自动测试中保留大文件。
- 记录模型版本、运行命令、耗时和已知限制，不修改 `voice/CosyVoice` 内部实现。

**测试与验收：**

- 三个真实任务均到达 ready，音频可播放，应用未直接调用适配层以外的 CosyVoice。
- 测试产生的数据库记录和文件通过正式删除服务清理。

#### J04 部署与维护文档

**前置依赖：** J02、J03。

**本次实现：**

- 编写从空环境到启动服务的步骤，包括 Node.js LTS、前端依赖和构建、Python 环境变量、迁移、目录权限、Web 和 worker。
- 说明占位 OIDC 与真实 OIDC 的替换边界、占位 LLM 与真实 LLM 的替换边界。
- 说明备份数据库与 `data/` 必须使用一致快照，以及恢复后运行一致性扫描。
- 列出 SQLite 单机限制、单 GPU worker 限制、日志位置、常见故障和安全上线检查。

**测试与验收：**

- 按文档在新临时数据库和空数据目录启动成功。
- 文档命令均实际执行验证，且都包含规定的环境变量和虚拟环境步骤。

## 6. 每个子任务的统一完成标准

每次实施会话除完成对应条目外，还必须满足以下条件：

1. 先读取本任务涉及目录下的 `AGENTS.md`，运行 `git status --short` 确认已有改动，不修改无关代码或注释。
2. 数据库结构变化必须同时包含 ORM、迁移和约束测试。
3. API 变化必须包含 schema、权限、错误码和集成测试。
4. 文件操作必须使用临时文件、原子替换、失败清理和受控路径。
5. 关键操作使用 Loguru 写文件日志，警告和异常同时输出终端。
6. 新增业务代码至少通过定向测试、格式检查和静态检查；共享代码变化运行完整普通测试集。
7. 除 J03 外不得在自动测试中加载 CosyVoice、CUDA 或 vLLM。
8. 更新与本次行为直接相关的 README/API 文档，不删除无关注释。
9. 验证通过后只暂存本任务涉及的文件，检查 staged diff，按约定创建一个独立 Git 提交；不得提交运行数据、生成产物、凭证或其他人的工作区改动。
10. 结束时报告修改文件、迁移影响、测试命令、测试结果、提交哈希和仍未实施的下一编号任务。

## 7. 推荐执行顺序与里程碑

- **里程碑 1，基础可运行：** A01-A05、B01-B04。具备后端应用、Vue 前端骨架、数据库、日志、占位登录和授权。
- **里程碑 2，资源模型完成：** 按 C01-C03、D01-D02、E01、C04-C05、D03-D04、E02-E03、D05 的依赖顺序执行。完成后具备两套标签、音色和音频的完整后端模型。
- **里程碑 3，首个可用流程：** D06、E04、F01-F05。教师可建音色和生成单人/多人音频，学生可访问公开音频。
- **里程碑 4，需求功能完整：** G01-G04、H01-H03、I01-I02。具备语料批量生成、组卷、管理和多语言展示。
- **里程碑 5，可交付：** I03-I05、J01-J04。完成一致性、安全、运维、浏览器和真实 GPU 验证。

## 8. 实施前需确认但不阻塞规划的假设

以下内容在原需求中没有完全定义，本规划采用了保守默认值。若产品决定不同，应在开始相关阶段前修改规划和数据模型：

- `userId` 首次设置后不可修改；大小写不敏感但保留首次输入用于展示。
- 新音色和音频默认私有；只有所有者能修改和删除。
- 公开音色可被其他教师用于生成，但匿名学生不浏览音色库。
- 使用公开音频作为音色示例只改变展示播放器，不重新提取音色模型。
- 被音色示例或组卷引用的音频不能直接删除或改为私有，必须先解除引用。
- 标签由教师按需创建，author 标签由系统独占管理；首版没有管理员角色和标签审核流。
- 搜索词采用 AND；完整标签精确匹配，普通关键词对标题执行不区分大小写的规范化子串匹配，也可匹配标签值；结果默认按创建时间倒序并以 ID 作为稳定次排序。
- 批量生成允许部分成功；失败条目可单独重试。
- 组卷结果创建为普通音频，因此可以继续设置标签、公开和加入其他组卷，但服务层需要阻止循环来源关系。
- 首版运行形态为单机数据库、单个 GPU worker；需要多机部署时再引入 PostgreSQL 和外部队列。
